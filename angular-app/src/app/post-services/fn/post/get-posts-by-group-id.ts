/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedModelPost } from '../../models/paged-model-post';

export interface GetPostsByGroupId$Params {
  'group-id': string;
  page?: number;
  size?: number;
}

export function getPostsByGroupId(http: HttpClient, rootUrl: string, params: GetPostsByGroupId$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedModelPost>> {
  const rb = new RequestBuilder(rootUrl, getPostsByGroupId.PATH, 'get');
  if (params) {
    rb.path('group-id', params['group-id'], {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedModelPost>;
    })
  );
}

getPostsByGroupId.PATH = '/api/v1/posts/group/{group-id}';
